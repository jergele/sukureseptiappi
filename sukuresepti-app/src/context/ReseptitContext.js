import { createContext } from "react";
const ReseptitContext = createContext();
export default ReseptitContext;

// Luodaan/määritellään context. Saa arvoja Providerilta.
// Otetaan käyttöön useContextilla ->
// sitten pitää provide context eli tarjota se käyttöön -> GlobalState.js
